Brain:
  reward: unsupervised
  batch_size: 512
  buffer_size: 2048
  algorithm: PPO
  policy: CnnPolicy
  seed: 42
  encoder: small
  train_encoder: True
Body:
  type: basic
  wrappers: []
  dvs: False
Environment:
  use_ship: True
  background: A
  # Use and absolute path
  executable_path: "/data/wjpeacoc/builds/parsing/ParsingBuild.x86_64"
  record_agent: True
  record_chamber: True
  recording_frames: 1000
Config:
  run_id: "parsing_trial_1"
  num_brains: 5
  mode: full
  train_eps: 10
  test_eps: 5
  # Use and absolute path
  output_dir: "/data/wjpeacoc/experiments/parsing_trial_data/"
