Brain:
  reward: unsupervised
  batch_size: 512
  buffer_size: 2048
  algorithm: PPO
  policy: CnnPolicy
  seed: 42
  encoder: small
  train_encoder: True
Body:
  type: basic
  dvs: False
Environment:
  # condition
  imprinting_condition: "ChickBiologicalMotion"
  test_condition: ""
  target_video: "biomo.webm"
  nontarget_video: "White.webm"
  # generic config
  executable_path: "/data/wjpeacoc/builds/biomotion.x86_64"
  record_chamber: False
  record_agent: False
  recording_frames: 100
Config:
  run_id: "biomotion-test1"
  num_brains: 5
  mode: "full"
  train_eps: 1000
  test_eps: 10
  output_dir: "/data/wjpeacoc/experiments/results/biomotion/"
